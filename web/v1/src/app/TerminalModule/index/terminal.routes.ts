import { TerminalComponent } from './terminal.component';
import { Routes } from '@angular/router';

export const TERMINAL_ROUTES_MAIN: Routes = [
    {
        path: 'terminal',
        component: TerminalComponent
    }
]