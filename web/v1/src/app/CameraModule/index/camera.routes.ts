import { CameraComponent } from './camera.component';
import { Routes } from '@angular/router';


export const CAMERA_ROUTES_MAIN: Routes = [
    {
        path: 'camera',
        component: CameraComponent
    }
]